{%- from "govuk/components/table/macro.njk" import govukTable -%}
{% from "./formField.njk" import formField %}

{% macro formSubsection(title, data, isTableData = false) %}

    <div class="govuk-form-group govuk-summary-card govuk-!-margin-bottom-6">
        <div class="govuk-summary-card__title-wrapper">
            <h3 class="govuk-summary-card__title ">{{ title }}</h3>
        </div>
        <div class="govuk-summary-card__content">
            {% if isTableData %}
                <div class="govuk-table-responsive">
                    {{ govukTable(data) }}
                </div>
            {% else %}
                <dl class="govuk-summary-list">
                    {% for field in data %}
                        {{ formField(field) }}
                    {% endfor %}
                </dl>
            {% endif %}
        </div>
    </div>

{% endmacro %}
